[package]
name = "tracker"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "*", features = ["derive"] }
clap_complete = "*"
chrono = "*"
directories = "*"
num-traits = "*"
regex = "*"
serde = { version = "*", features = ["derive"] }
temp-dir = "*"
toml = "*"
